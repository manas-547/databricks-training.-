# What I Did in NULL Functions Practice

* Created sample SQL tables with NULL values

* Inserted records containing missing data

* Practiced handling NULL values in SQL

* Used `IFNULL()` to replace NULL values with default values

* Used `COALESCE()` to return the first non-null value

* Used `NULLIF()` for conditional NULL conversion

* Used `IS NULL` to find missing values

* Used `IS NOT NULL` to find available values

* Replaced NULL salaries, bonuses, amounts, and stock values

* Calculated total earnings using NULL-safe logic

* Prevented divide-by-zero errors using `NULLIF()`

* Filtered records containing NULL and non-NULL values

* Worked on real-time scenarios like:

  * Employee salary handling
  * Product stock checking
  * Order payment calculations
  * Bonus and discount analysis

* Improved understanding of SQL NULL handling concepts

* Practiced beginner to advanced NULL function queries for placements and interviews
